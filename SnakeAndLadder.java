public class SnakeAndLadder {
    public static void main(String[] args) {
        int dice = (int) (Math.random() * 10) % 6 + 1;
        System.out.println(" dice roll to get The number Between1 to 6 : " + dice);
    }
}
